# Web UI
streamlit==1.62.0

# RAG framework
langchain-core==1.6.0
langchain-community==0.4.2
langchain-text-splitters==1.1.2
langchain-chroma==1.1.0

# Vector database
chromadb==1.5.9

# Embeddings - runs locally via ONNX, no API key needed
fastembed==0.8.0

# Answer generation
langchain-google-genai==4.3.5

# Loads .env for local development
python-dotenv==1.2.3

# Streamlit Cloud's system sqlite3 is older than Chroma requires.
# Linux-only: this wheel does not exist for Windows and isn't needed there.
pysqlite3-binary==0.5.4; sys_platform == "linux"
